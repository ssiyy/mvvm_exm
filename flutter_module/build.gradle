// Top-level build file where you can add configuration options common to all sub-projects/modules.


task buildRunner(type:Exec){
    group "mvvm"
    description "build_runner"
    commandLine "cmd","/c","flutter packages pub run build_runner build"

    doLast {
        def outputStr = standardOutput.toString()
        println outputStr
    }
}
